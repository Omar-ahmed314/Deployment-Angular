# Set the environment variables for the EB before the deployment
eb init $AWS_APPLICATION_NAME --platform $AWS_PLATFORM --region $AWS_DEFAULT_REGION
eb list
eb use $AWS_APPLICATION_ENV
eb setenv POSTGRES_USERNAME=$POSTGRES_USERNAME POSTGRES_PASSWORD=$POSTGRES_PASSWORD POSTGRES_DB=$POSTGRES_DB POSTGRES_HOST=$POSTGRES_HOST POSTGRES_PORT=$POSTGRES_PORT JWT_SECRET=$JWT_SECRET PORT=$PORT
eb deploy